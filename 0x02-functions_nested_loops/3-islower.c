#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 1 (Success)
 */

int main(void)
{
	int _islower(int c);
	int j = 1, k = 0;
	char c = 'a';

	if (_islower(c))
	{
		_putchar(j + '0');
	}
	else
	{
		_putchar(k + '0');
	}

	return (0);
}
