#include "main.h"

/**
 * more_numbers - Printing more numbers than usual
 *
 * Return: Nothing
 */

void more_numbers(void)
{
	int i, k;
	char num;

	for (i = 0; i < 10; i++)
	{
		num = k + '0';
		for (k = 0; k <= 14; k++)
		{
			_putchar(num);
		}
		_putchar('\n');
	}
}
