#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *main - Entry point
 *Description: Printing single digit numbers
 *Return: Always 0 (Success)
 */

int main(void)
{
	int d;

	for (d = 0; d <= 9; d++)
	{
		printf("%d\n", d);
	}

	return (0);
}
